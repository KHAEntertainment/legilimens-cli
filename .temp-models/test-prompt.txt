You MUST respond with ONLY valid JSON. No explanations, no prose, no markdown - just pure JSON.

The JSON must match this schema:
{
  "shortDescription": string,  // 2-3 sentence description (min 10 chars)
  "features": string[]         // 3-10 key features/capabilities
}

Generate a JSON description for the React framework.

REMINDER: Output ONLY the JSON object. Start with { and end with }. Nothing else.
