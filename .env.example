# Legilimens Environment Configuration
#
# FIRST-TIME USERS: Run 'pnpm --filter @legilimens/cli start' to launch
# the setup wizard, which will guide you through configuration.
#
# ADVANCED USERS: You can manually configure by copying this file to .env
# or by editing ~/.legilimens/config.json directly.
#
# Environment variables take precedence over config file settings.

# API Keys for documentation fetchers
FIRECRAWL_API_KEY=
CONTEXT7_API_KEY=
REFTOOLS_API_KEY=

# Optional: Fetcher configuration
# Timeout in milliseconds (default: 60000)
LEGILIMENS_FETCHER_TIMEOUT_MS=60000

# Maximum retry attempts (default: 2)
LEGILIMENS_MAX_RETRIES=2

# Optional: Project directories
# LEGILIMENS_ROOT=
# LEGILIMENS_DOCS_DIR=docs
# LEGILIMENS_CONSTITUTION_DIR=.specify/memory

# AI CLI Tool Configuration
# Preferred AI CLI tool for gateway documentation generation
# Options: gemini-cli, openai, claude-code, qwen-code
# If not set, will auto-detect and use first available tool
# Note: Setup wizard will help detect and configure tools
LEGILIMENS_AI_CLI_TOOL=gemini-cli

# Timeout for AI CLI tool execution (milliseconds)
# Default: 30000 (30 seconds)
LEGILIMENS_AI_CLI_TIMEOUT_MS=30000

# Enable or disable AI generation (falls back to static content if disabled)
# Default: true
LEGILIMENS_AI_GENERATION_ENABLED=true

# Optional: Override the command used to invoke the AI CLI tool
# Useful if the tool is installed in a non-standard location or not on PATH
# When set with LEGILIMENS_AI_CLI_TOOL, the preferred tool will be attempted
# even if not detected on PATH
# Example: LEGILIMENS_AI_CLI_COMMAND_OVERRIDE=/custom/path/to/gemini-cli
# LEGILIMENS_AI_CLI_COMMAND_OVERRIDE=

# Skip setup wizard on first run (for CI/automation)
# LEGILIMENS_SKIP_SETUP=true
